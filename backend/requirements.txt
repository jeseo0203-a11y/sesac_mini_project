# (pip용) Poetry를 쓰면 pyproject.toml/poetry.lock 사용 — 이 파일은 pip 대안입니다.
# Railway 백엔드 서비스의 Root Directory를 backend 로 두면 이 파일로 빌드됩니다.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.27.0
PyJWT==2.8.0
openai==1.40.0
numpy==1.26.4
psycopg2-binary==2.9.9
